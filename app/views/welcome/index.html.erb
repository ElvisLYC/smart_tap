
<%= stylesheet_link_tag 'welcome' %>

<% if signed_in? -%>

	<%= render partial: '/welcome/signed_in' %>

<% else %>

	<%= render partial: '/welcome/signed_out' %>

<% end %>

<%= link_to "Sign in with Google", "/auth/google_oauth2" %>
